import { FramingBehavior as BabylonFramingBehavior } from '@babylonjs/core';

export type IFramingBehaviorProps = {}

export const P2PFramingBehavior = (props: IFramingBehaviorProps) => {
    return null;
};